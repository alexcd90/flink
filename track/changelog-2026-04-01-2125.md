# Apache Flink 研发进展 - 2026-04-01 21:25

> 拉取时间：2026-04-01 21:25
> 新增 commit 数量：21
> commit 范围：60ce5de..77266f0

---

## 变更列表

### 1. Fix | table-planner | Fix field names in code generation for JSON_OBJECT/ARRAY/STRING

- **commit**：`64007b1`（作者：Timo Walther，时间：2026-03-31）
- **类型**：Fix
- **摘要**：修复 JSON_OBJECT/ARRAY/STRING 函数在代码生成阶段字段名错误的问题，仅修改 `JsonGenerateUtils.scala` 中 1 处逻辑，并补充了对应的集成测试用例。
- **关键文件**：
  - `flink-table/flink-table-planner/src/main/scala/org/apache/flink/table/planner/codegen/JsonGenerateUtils.scala`：修复代码生成时 JSON 函数字段名命名错误
  - `flink-table/flink-table-planner/src/test/java/org/apache/flink/table/planner/functions/JsonFunctionsITCase.java`：新增 46 行集成测试验证修复

---

### 2. Feature | Runtime/REST | Add configurable TTL for ExecutionGraph cache independent of web refresh interval

- **commit**：`e890caf`（作者：Myracle，时间：2026-02-03）
- **类型**：Feature
- **摘要**：新增 `rest.cache.execution-graph.timeout` 配置项，允许独立配置 ExecutionGraph 缓存的 TTL，不再强制跟随 `web.refresh-interval`。未配置时默认回退至 `web.refresh-interval`，设置为 0 时强制每次拉取最新数据，适用于需要实时状态同步的场景。
- **关键文件**：
  - `flink-core/src/main/java/org/apache/flink/configuration/RestOptions.java`：新增 `CACHE_EXECUTION_GRAPH_TIMEOUT` 配置项
  - `flink-runtime/src/main/java/org/apache/flink/runtime/rest/handler/RestHandlerConfiguration.java`：新增 `executionGraphCacheTTL` 字段及配置解析逻辑
  - `flink-runtime/src/main/java/org/apache/flink/runtime/rest/RestEndpointFactory.java`：使用新的 TTL 方法替代原来直接使用 refreshInterval
  - `flink-runtime/src/test/java/org/apache/flink/runtime/rest/handler/RestHandlerConfigurationTest.java`：新增参数化测试覆盖默认值、自定义值、负值校验
  - `docs/layouts/shortcodes/generated/rest_configuration.html`：更新文档配置表格

---

### 3. Refactor | S3 | Add @Experimental annotation to public S3 classes

- **commit**：`c0265db`（作者：Samrat002，时间：2026-03-26）
- **类型**：Refactor
- **摘要**：为 `flink-s3-fs-native` 模块中 8 个公开 S3 类添加 `@Experimental` 注解，标记其 API 稳定性状态，无功能逻辑变更。
- **关键文件**：
  - `flink-filesystems/flink-s3-fs-native/src/main/java/org/apache/flink/fs/s3native/NativeS3AFileSystemFactory.java`：添加 @Experimental 注解
  - `flink-filesystems/flink-s3-fs-native/src/main/java/org/apache/flink/fs/s3native/NativeS3FileSystemFactory.java`：添加 @Experimental 注解
  - `flink-filesystems/flink-s3-fs-native/src/main/java/org/apache/flink/fs/s3native/writer/NativeS3RecoverableWriter.java`：添加 @Experimental 注解

---

### 4. Feature | configuration/runtime | Introduce the rescale history configuration option

- **commit**：`897f8ed`（作者：Pan Yuepeng，时间：2025-07-30）
- **类型**：Feature
- **摘要**：在 `WebOptions` 中新增 `web.adaptive-scheduler.rescale-history.size` 配置项，控制 Adaptive Scheduler 每个 Job 保留的弹性扩缩容历史记录最大数量；配置值 ≤0 时禁用该功能，并将其接入 `AdaptiveScheduler` 的构造参数。
- **关键文件**：
  - `flink-core/src/main/java/org/apache/flink/configuration/WebOptions.java`：新增 `MAX_ADAPTIVE_SCHEDULER_RESCALE_HISTORY_SIZE` 配置项
  - `flink-runtime/src/main/java/org/apache/flink/runtime/scheduler/adaptive/AdaptiveScheduler.java`：将新配置项传入 RescaleTimeline 构造参数，替代原来硬编码的 -1
  - `docs/layouts/shortcodes/generated/all_jobmanager_section.html`：更新配置文档

---

### 5. Feature | runtime | Support store rescale history with statistics by ExecutionGraphInfo

- **commit**：`ab3bcce`（作者：Yuepeng Pan，时间：2026-02-26）
- **类型**：Feature
- **摘要**：在 `ExecutionGraphInfo` 中集成弹性扩缩容历史统计信息（`RescalesSummarySnapshot`），新增 `RescalesSummary`、`RescalesSummarySnapshot` 等数据模型，`RescaleTimeline` 支持通过限定容量存储扩缩容历史记录，并扩展了集成测试（新增约 700 行测试代码）。diff 超 500 行，仅依据 stat 分析。
- **关键文件**：
  - `flink-runtime/src/main/java/org/apache/flink/runtime/scheduler/ExecutionGraphInfo.java`：集成 rescale 统计快照
  - `flink-runtime/src/main/java/org/apache/flink/runtime/scheduler/adaptive/timeline/RescalesSummarySnapshot.java`：新增 rescale 统计快照数据结构（+88 行）
  - `flink-runtime/src/main/java/org/apache/flink/runtime/scheduler/adaptive/timeline/DefaultRescaleTimeline.java`：支持有界容量存储历史记录
  - `flink-runtime/src/main/java/org/apache/flink/runtime/scheduler/adaptive/timeline/RescalesSummary.java`：新增汇总数据类
  - `flink-runtime/src/test/java/org/apache/flink/runtime/scheduler/adaptive/timeline/RescaleTimelineITCase.java`：大规模扩展集成测试

---

### 6. Fix | table | Constraint and watermark should stay while CoA MT if there is no explicit change

- **commit**：`dc6d128`（作者：Sergey Nuyanzin，时间：2026-03-31）
- **类型**：Fix
- **摘要**：修复 `CREATE OR ALTER MATERIALIZED TABLE` 语句在未显式指定 schema 或约束定义时，会错误地删除已有约束（PRIMARY KEY）和 watermark 的 Bug。新增 `hasConstraintDefinition()` 判断逻辑，仅在用户明确指定约束时才触发约束变更。
- **关键文件**：
  - `flink-table/flink-sql-parser/src/main/java/org/apache/flink/sql/parser/ddl/materializedtable/SqlCreateMaterializedTable.java`：新增 `getTableConstraints()` 方法
  - `flink-table/flink-table-planner/src/main/java/org/apache/flink/table/planner/operations/converters/materializedtable/AbstractCreateMaterializedTableConverter.java`：接口新增 `hasConstraintDefinition()` 方法
  - `flink-table/flink-table-planner/src/main/java/org/apache/flink/table/planner/operations/converters/materializedtable/SqlCreateOrAlterMaterializedTableConverter.java`：实现约束/watermark 变更的按需触发逻辑
  - `flink-table/flink-table-planner/src/test/java/org/apache/flink/table/planner/operations/SqlNodeToOperationSqlCreateOrAlterMaterializedTableConverterTest.java`：新增测试用例验证无 schema 定义时约束保持不变

---

### 7. Fix | table | Fix MultiJoinStateViews by changing GenericRowData to BinaryRowData

- **commit**：`0c4ab25`（作者：Dmitriy Linevich，时间：2026-03-31）
- **类型**：Fix
- **摘要**：修复 MultiJoin 在使用 Heap StateBackend 时，因 `GenericRowData` 与 `BinaryRowData` 序列化不兼容导致的状态读写错误。通过引入 `requiresKeyDeepCopy` 参数，在需要深拷贝的场景下将组合 key 转换为 `BinaryRowData`，同时修复 `StreamingMultiJoinOperator` 的参数传递缺失问题。
- **关键文件**：
  - `flink-table/flink-table-runtime/src/main/java/org/apache/flink/table/runtime/operators/join/stream/state/MultiJoinStateViews.java`：新增深拷贝逻辑，内部类 `InputSideHasUniqueKey` 和 `InputSideHasNoUniqueKey` 均支持 key 深拷贝
  - `flink-table/flink-table-runtime/src/main/java/org/apache/flink/table/runtime/operators/join/stream/StreamingMultiJoinOperator.java`：修复 `requiresKeyDeepCopy` 参数传递缺失
  - `flink-table/flink-table-planner/src/test/scala/org/apache/flink/table/planner/runtime/stream/sql/JoinITCase.scala`：新增三路 MultiJoin 无主键场景的集成测试

---

### 8. Refactor | table | Add `Interval` to reuse code for `FRESHNESS` and `START MODE`

- **commit**：`64a21e7`（作者：Sergey Nuyanzin，时间：2026-03-31）
- **类型**：Refactor
- **摘要**：从 `IntervalFreshness` 中抽取通用的 `Interval` 类，统一 `FRESHNESS` 和 `START MODE` 的时间区间表示逻辑，减少重复代码。修改涉及 14 个文件（491 行新增、202 行删减），diff 超 500 行，仅依据 stat 分析。
- **关键文件**：
  - `flink-table/flink-table-common/src/main/java/org/apache/flink/table/catalog/Interval.java`：新增通用 `Interval` 数据类（+173 行）
  - `flink-table/flink-table-common/src/main/java/org/apache/flink/table/catalog/IntervalFreshness.java`：重构使用新的 `Interval` 基类
  - `flink-table/flink-table-planner/src/main/java/org/apache/flink/table/planner/utils/MaterializedTableUtils.java`：更新工具类以使用新抽象

---

### 9. Docs | table | Update documentation to cover new CREATE/ALTER operations for MATERIALIZED TABLEs

- **commit**：`34246de`（作者：Sergey Nuyanzin，时间：2026-03-31）
- **类型**：Docs
- **摘要**：更新 SQL 文档，补充物化表 `CREATE`/`ALTER`/`DROP` 操作的新增内容，中英双语文档同步更新，新增约 270 行内容。
- **关键文件**：
  - `docs/content/docs/sql/materialized-table/statements.md`：英文物化表 SQL 语句文档（+128 行）
  - `docs/content.zh/docs/sql/materialized-table/statements.md`：中文物化表 SQL 语句文档（+126 行）
  - `docs/content/docs/sql/reference/ddl/alter.md`：英文 ALTER 文档补充
  - `docs/content.zh/docs/sql/reference/ddl/create.md`：中文 CREATE 文档补充

---

### 10. Fix | table | `LIKE` clause doesn't support some patterns

- **commit**：`b975641`（作者：Sergey Nuyanzin，时间：2026-03-31）
- **类型**：Fix
- **摘要**：修复 SQL `LIKE` 子句对某些模式（pattern）不支持的问题，重构 `LikeCallGen.scala` 代码生成逻辑，修复 `SqlLikeChainChecker`，并新增大量集成测试（+209 行）。
- **关键文件**：
  - `flink-table/flink-table-planner/src/main/scala/org/apache/flink/table/planner/codegen/calls/LikeCallGen.scala`：重构 LIKE 代码生成逻辑（+85 行改动）
  - `flink-table/flink-table-runtime/src/main/java/org/apache/flink/table/runtime/functions/SqlLikeChainChecker.java`：修复链式 LIKE 检查逻辑
  - `flink-table/flink-table-planner/src/test/java/org/apache/flink/table/planner/functions/LikeFunctionITCase.java`：新增 209 行集成测试
  - `flink-table/flink-table-common/src/main/java/org/apache/flink/table/functions/SqlLikeUtils.java`：工具类微调

---

### 11. Feature | table | Support `SHOW CREATE OR ALTER MATERIALIZED TABLE`

- **commit**：`8b71634`（作者：Sergey Nuyanzin，时间：2026-03-31）
- **类型**：Feature
- **摘要**：为物化表新增 `SHOW CREATE OR ALTER MATERIALIZED TABLE` 语法支持，扩展 SQL 解析器、ShowCreate 工具类和 Operation 类，更新中英文文档，并重写 `ShowCreateUtilTest`。
- **关键文件**：
  - `flink-table/flink-sql-parser/src/main/codegen/includes/parserImpls.ftl`：SQL 语法文件扩展
  - `flink-table/flink-sql-parser/src/main/java/org/apache/flink/sql/parser/dql/SqlShowCreateMaterializedTable.java`：解析器节点更新
  - `flink-table/flink-table-api-java/src/main/java/org/apache/flink/table/api/internal/ShowCreateUtil.java`：生成 SHOW CREATE OR ALTER 语句逻辑
  - `flink-table/flink-table-planner/src/test/java/org/apache/flink/table/api/internal/ShowCreateUtilTest.java`：测试重写（247 行改动）
  - `docs/content/docs/sql/reference/utility/show.md`：文档同步更新

---

### 12. Refactor | checkpoint | Rename isUnalignedDuringRecoveryEnabled to isCheckpointingDuringRecoveryEnabled

- **commit**：`17dfa5f`（作者：Rui Fan，时间：2026-03-26）
- **类型**：Refactor
- **摘要**：将 `CheckpointingOptions` 中的方法 `isUnalignedDuringRecoveryEnabled` 重命名为 `isCheckpointingDuringRecoveryEnabled`，使命名更准确地反映其语义，同步更新测试。
- **关键文件**：
  - `flink-core/src/main/java/org/apache/flink/configuration/CheckpointingOptions.java`：方法重命名
  - `flink-core/src/test/java/org/apache/flink/configuration/CheckpointingOptionsTest.java`：测试同步更新

---

### 13. Feature | checkpoint | Filtering record before processing without spilling strategy

- **commit**：`23ebbb5`（作者：Rui Fan，时间：2026-02-18）
- **类型**：Feature
- **摘要**：在 checkpoint 对齐恢复路径中，新增基于过滤策略的记录处理机制，无需溢写即可在恢复阶段过滤掉旧数据，新增 `ChannelStateFilteringHandler`、`RecordFilterContext`、`VirtualChannelRecordFilterFactory` 等关键组件。diff 超 2000 行，仅依据 stat 分析。
- **关键文件**：
  - `flink-streaming-java/src/main/java/org/apache/flink/streaming/runtime/io/recovery/ChannelStateFilteringHandler.java`：新增过滤处理器（+458 行）
  - `flink-streaming-java/src/main/java/org/apache/flink/streaming/runtime/io/recovery/RecordFilterContext.java`：新增过滤上下文（+227 行）
  - `flink-streaming-java/src/main/java/org/apache/flink/streaming/runtime/io/recovery/VirtualChannelRecordFilterFactory.java`：新增虚拟通道过滤工厂（+123 行）
  - `flink-streaming-java/src/main/java/org/apache/flink/streaming/runtime/tasks/StreamTask.java`：集成过滤机制到 StreamTask（+82 行）
  - `flink-runtime/src/main/java/org/apache/flink/runtime/checkpoint/channel/RecoveredChannelStateHandler.java`：恢复通道状态处理逻辑调整

---

### 14. Fix | checkpoint | Use heap buffer fallback to avoid deadlock during recovery

- **commit**：`1b933bd`（作者：Rui Fan，时间：2026-03-27）
- **类型**：Fix
- **摘要**：修复恢复阶段因 Buffer 池耗尽导致的死锁问题：在过滤恢复模式下，`RecoveredInputChannel` 在申请 Buffer 时若池已满，回退到使用堆内存（Heap Buffer），绕过 Buffer 池死锁；同时在 `IndexedInputGate` 和 `SingleInputGate` 中新增 `isCheckpointingDuringRecoveryEnabled` 开关控制。
- **关键文件**：
  - `flink-runtime/src/main/java/org/apache/flink/runtime/io/network/partition/consumer/RecoveredInputChannel.java`：添加堆内存回退逻辑（+26 行）
  - `flink-runtime/src/main/java/org/apache/flink/runtime/io/network/partition/consumer/IndexedInputGate.java`：新增抽象方法 `setCheckpointingDuringRecoveryEnabled`/`isCheckpointingDuringRecoveryEnabled`
  - `flink-runtime/src/main/java/org/apache/flink/runtime/io/network/partition/consumer/SingleInputGate.java`：实现上述接口（+12 行）
  - `flink-streaming-java/src/main/java/org/apache/flink/streaming/runtime/tasks/StreamTask.java`：传递开关标志至 InputGate

---

### 15. Fix | table | `CurrentDatabase` fails for some patterns

- **commit**：`9b2a112`（作者：Sergey Nuyanzin，时间：2026-03-31）
- **类型**：Fix
- **摘要**：修复 `CurrentDatabase` 函数在某些查询模式下执行失败的问题，调整 `CodeGeneratorContext` 中相关逻辑，并补充批处理集成测试。
- **关键文件**：
  - `flink-table/flink-table-planner/src/main/scala/org/apache/flink/table/planner/codegen/CodeGeneratorContext.scala`：修复 CurrentDatabase 代码生成问题
  - `flink-table/flink-table-planner/src/test/scala/org/apache/flink/table/planner/runtime/batch/table/CalcITCase.scala`：补充测试场景

---

### 16. Test | tests | Fix flaky ChainUnionTest by tolerating CHECKPOINT_COORDINATOR_SUSPEND exception

- **commit**：`f302c47`（作者：Shekhar Prasad Rajak，时间：2026-04-01）
- **类型**：Test
- **摘要**：修复 `ChainUnionTest` 偶发性失败问题，在 `AbstractOperatorRestoreTestBase` 中容忍 `CHECKPOINT_COORDINATOR_SUSPEND` 异常，避免测试因 checkpoint 协调器暂停导致误报失败。
- **关键文件**：
  - `flink-tests/src/test/java/org/apache/flink/test/state/operator/restore/AbstractOperatorRestoreTestBase.java`：新增异常容忍逻辑（+2 行）

---

### 17. Feature | runtime | Check terminated applications for duplication

- **commit**：`2259862`（作者：Yi Zhang，时间：2026-03-18）
- **类型**：Feature
- **摘要**：在 `Dispatcher` 中新增对已终止应用的重复提交检查，防止同一 Job 在已完成/失败后再次被接受。引入 `ApplicationResultStore` 抽象及 `FileSystemApplicationResultStore` 实现，支持持久化存储终止应用状态，`ApplicationStatus` 扩展了终止判断方法。diff 超 500 行，仅依据 stat 分析。
- **关键文件**：
  - `flink-runtime/src/main/java/org/apache/flink/runtime/dispatcher/Dispatcher.java`：新增重复提交检测逻辑（+275 行）
  - `flink-runtime/src/main/java/org/apache/flink/runtime/highavailability/ApplicationResultStore.java`：新增结果存储接口（+17 行）
  - `flink-runtime/src/main/java/org/apache/flink/runtime/highavailability/FileSystemApplicationResultStore.java`：文件系统实现（+17 行）
  - `flink-runtime/src/main/java/org/apache/flink/clusterframework/ApplicationStatus.java`：新增终止状态判断方法（+25 行）
  - `flink-runtime/src/test/java/org/apache/flink/runtime/dispatcher/DispatcherApplicationTest.java`：新增应用测试（+117 行）

---

### 18. Fix | runtime | Fix DispatcherCleanupITCase.testCleanupAfterLeadershipChange

- **commit**：`923a359`（作者：Yi Zhang，时间：2026-03-27）
- **类型**：Fix
- **摘要**：修复 `DispatcherCleanupITCase` 中 `testCleanupAfterLeadershipChange` 偶发性测试失败，简化测试逻辑（-6 行）。
- **关键文件**：
  - `flink-runtime/src/test/java/org/apache/flink/runtime/dispatcher/DispatcherCleanupITCase.java`：修复测试逻辑（2 增 6 删）

---

### 19. Fix | rpc | Mask sensitive values in Pekko debug configuration logs

- **commit**：`738dd00`（作者：Vishal，时间：2026-04-01）
- **类型**：Fix
- **摘要**：在 Pekko ActorSystem 启动时打印调试配置日志时，对敏感信息（如 SSL 密钥密码、证书密码等）进行脱敏处理，新增 `toMaskedMap()` 工具方法，使用 `ConfigurationUtils.hideSensitiveValues()` 进行屏蔽。
- **关键文件**：
  - `flink-rpc/flink-rpc-akka/src/main/java/org/apache/flink/runtime/rpc/pekko/ActorSystemBootstrapTools.java`：新增 `toMaskedMap()` 方法，替换原始 config 输出（+23 行）
  - `flink-rpc/flink-rpc-akka/src/test/java/org/apache/flink/runtime/rpc/pekko/ActorSystemBootstrapToolsTest.java`：新增脱敏测试（+36 行）

---

### 20. Docs | REST | Rebuild REST API docs pages to supplement the missing content

- **commit**：`b631cca`（作者：Yuepeng Pan，时间：2026-04-01）
- **类型**：Docs
- **摘要**：重建 REST API 文档页面，补充缺失内容；更新 `rest_v1_dispatcher.html` 和 `rest_v1_dispatcher.yml`，以及 SQL Gateway 多版本（v1-v4）的 YAML 文档。
- **关键文件**：
  - `docs/layouts/shortcodes/generated/rest_v1_dispatcher.html`：REST Dispatcher 文档更新（28 行改动）
  - `docs/static/generated/rest_v1_dispatcher.yml`：REST v1 Dispatcher YAML 文档（36 行改动）
  - `docs/static/generated/rest_v1_sql_gateway.yml`：SQL Gateway v1 文档（+22 行）
  - `docs/static/generated/rest_v2_sql_gateway.yml`：SQL Gateway v2 文档（+22 行）

---

### 21. Fix | kubernetes | Fix the option typo in KubernetesConfigOptions

- **commit**：`77266f0`（作者：Yuepeng Pan，时间：2026-04-01）
- **类型**：Fix
- **摘要**：修复 `KubernetesConfigOptions` 中配置项 key 的拼写错误（typo），同步修正 `Fabric8FlinkKubeClient` 和 `KubernetesTestBase` 中对该 key 的引用。
- **关键文件**：
  - `flink-kubernetes/src/main/java/org/apache/flink/kubernetes/configuration/KubernetesConfigOptions.java`：修复配置 key 拼写错误（13 行改动）
  - `flink-kubernetes/src/main/java/org/apache/flink/kubernetes/kubeclient/Fabric8FlinkKubeClient.java`：更新 key 引用
  - `flink-kubernetes/src/test/java/org/apache/flink/kubernetes/KubernetesTestBase.java`：测试同步修正

---

## 模块统计

| 模块 | 新增 commit 数 | 类型分布 |
|------|--------------|---------|
| table | 9 | Feature×2, Fix×5, Refactor×1, Docs×1 |
| runtime | 4 | Feature×2, Fix×2 |
| checkpoint | 3 | Feature×1, Fix×1, Refactor×1 |
| docs | 2 | Docs×2 |
| rpc/pekko | 1 | Fix×1 |
| S3/filesystem | 1 | Refactor×1 |
| kubernetes | 1 | Fix×1 |
| REST/configuration | 1 | Feature×1 |
| tests | 1 | Test×1 |

**类型汇总**：Feature×6，Fix×10，Refactor×3，Docs×2，Test×1
