# Apache Flink 研发进展 - 2026-03-30 22:10

> 拉取时间：2026-03-30 22:10
> 新增 commit 数量：6
> commit 范围：1cfc7d37aba..47349536b09

---

## 变更列表

### 1. Feature | python | 在 Python UDX FunctionContext 中暴露运行时上下文信息

- **commit**：`d62be55c09a`（作者：yongliu，时间：2026-03-26）
- **JIRA**：FLINK-38933
- **类型**：Feature
- **摘要**：对齐 Python UDX 与 Java UDX 的能力，通过扩展 `FunctionContext` 新增 6 个 getter 方法（任务名、并行度、子任务索引、重试次数等），并通过 protobuf 协议在 Java 算子中将运行时上下文传递给 Python UDF，使 Python UDF 开发者可获取更完整的任务运行时信息。
- **关键文件**：
  - `flink-python/pyflink/table/udf.py`：FunctionContext 新增 6 个 getter 方法
  - `flink-python/pyflink/proto/flink-fn-execution.proto`：protobuf 协议新增 runtime_context 字段
  - `flink-python/pyflink/fn_execution/table/operations.py`：运行时上下文传递逻辑
  - `flink-python/.../python/util/ProtoUtils.java`：Java 侧 proto 工具类扩展
  - `flink-python/.../AbstractPythonStreamAggregateOperator.java`：流聚合算子上下文传递

---

### 2. Feature | python | `python.files` 配置项支持非 .zip 压缩格式

- **commit**：`c38b7e68633`（作者：RaoraoXiong，时间：2026-03-29）
- **JIRA**：FLINK-39260
- **类型**：Feature
- **摘要**：新增 `CompressionUtils` 工具类，扩展 `python.files` 配置项支持 `.tar.gz`、`.tar`、`.gz` 等多种压缩格式（原仅支持 `.zip`），同时更新 `AbstractPythonEnvironmentManager` 和 `PythonEnvUtils` 的文件解压逻辑，提升 Python 依赖包管理的灵活性。
- **关键文件**：
  - `flink-core/.../util/CompressionUtils.java`：新增压缩工具类（支持多格式）
  - `flink-python/.../client/python/PythonEnvUtils.java`：文件解压逻辑更新
  - `flink-python/.../env/AbstractPythonEnvironmentManager.java`：环境管理器解压更新
  - `flink-python/.../python/util/CompressionUtilsTest.java`：压缩工具类单元测试

---

### 3. Feature | table-planner | 利用不可变列推断 Sink 所需的 UpdateMode Trait

- **commit**：`8d8bf958a9a`（作者：xuyang，时间：2026-03-26）
- **JIRA**：FLINK-39339
- **类型**：Feature
- **摘要**：在 `FlinkChangelogModeInferenceProgram` 中引入不可变列信息来推断 Sink 所需的 update mode traits，当 Sink 上游节点的更新字段均为不可变列时，可将 `UPDATE_BEFORE + UPDATE_AFTER` 模式优化为仅 `INSERT`，减少 changelog 冗余数据，提升流处理性能。
- **关键文件**：
  - `flink-table-planner/.../FlinkChangelogModeInferenceProgram.scala`：不可变列 trait 推断逻辑（70行修改）
  - `flink-table-planner/.../ChangelogModeInferenceTest.xml`：46行新增测试用例
  - `flink-table-planner/.../ChangelogModeInferenceTest.scala`：119行新增测试逻辑

---

### 4. Feature | metrics | OpenTelemetry Reporter 支持批量 gRPC 导出

- **commit**：`b6cb13a52f1`（作者：Aleksandr Iushmanov，时间：2026-02-26）
- **JIRA**：FLINK-39126
- **类型**：Feature
- **摘要**：在 OpenTelemetry 指标 Reporter 中新增批量导出支持，引入可配置的 `batch.size` 参数将大量指标分批发送，避免单次请求超时或内存压力；采用异步完成追踪和单批次失败处理机制，提升大规模指标上报的稳定性。
- **关键文件**：
  - `flink-metrics-otel/.../OpenTelemetryMetricReporter.java`：批量导出核心逻辑（184行大幅修改）
  - `flink-metrics-otel/.../OpenTelemetryReporterOptions.java`：新增 batch.size 配置项
  - `flink-metrics-otel/.../OpenTelemetryMetricReporterTest.java`：新增完整单元测试（276行）
  - `flink-metrics-otel/.../OpenTelemetryMetricReporterITCase.java`：集成测试
  - `docs/.../metric_reporters.md`：文档更新

---

### 5. Feature | runtime | 自适应调度器支持记录弹性伸缩（Rescale）信息

- **commit**：`863140ea4a4`（作者：Yuepeng Pan，时间：2026-03-23）
- **JIRA**：FLINK-38342
- **类型**：Feature
- **摘要**：在自适应调度器（AdaptiveScheduler）中新增 Rescale 时间线记录能力，通过 `RescaleContext` 追踪每次弹性伸缩的触发时机、原因及结果；扩展 `StateTransitionManager` 和各调度状态类以传递和存储 rescale 信息，为 Web UI 的 Rescales 子页面提供后端数据支撑。
- **关键文件**：
  - `flink-runtime/.../adaptive/timeline/RescaleContext.java`：新增 RescaleContext 上下文类（33行）
  - `flink-runtime/.../adaptive/AdaptiveScheduler.java`：调度器 rescale 记录逻辑（107行扩展）
  - `flink-runtime/.../adaptive/DefaultStateTransitionManager.java`：状态转换管理扩展（69行）
  - `flink-runtime/.../adaptive/Executing.java`：执行状态 rescale 信息上报（70行）
  - `flink-runtime/.../adaptive/timeline/RescaleTimelineITCase.java`：新增集成测试（102行）

---

### 6. Feature | table | TO_CHANGELOG 支持 deletion flag

- **commit**：`47349536b09`（作者：Gustavo de Morais，时间：2026-03-30）
- **JIRA**：FLINK-39349
- **类型**：Feature
- **摘要**：在 `TO_CHANGELOG` 表值函数中新增 deletion flag 参数支持，允许用户指定某列作为删除标志位，当该列值为 true 时将行解释为 DELETE 消息，扩展了 `ToChangelogTypeStrategy` 的类型推断逻辑和 `ToChangelogFunction` 的运行时执行逻辑，同时补充了文档说明。
- **关键文件**：
  - `flink-table-planner/.../strategies/ToChangelogTypeStrategy.java`：类型推断支持 deletion flag（54行修改）
  - `flink-table-runtime/.../ptf/ToChangelogFunction.java`：运行时函数逻辑扩展
  - `flink-table-planner/.../exec/stream/ToChangelogTestPrograms.java`：测试程序（59行扩展）
  - `flink-table-common/.../api/PartitionedTable.java`：分区表接口小幅调整
  - `docs/.../queries/changelog.md`：文档补充 deletion flag 用法

---

## 模块统计

| 模块 | 新增 commit 数 | 类型分布 |
|------|--------------|---------|
| python | 2 | Feature×2 |
| table-planner | 2 | Feature×2 |
| metrics | 1 | Feature×1 |
| runtime | 1 | Feature×1 |

## 主题趋势

1. **Python UDF 能力对齐 Java**：运行时上下文暴露 + 多格式压缩包支持，持续补全 Python UDX 与 Java UDX 的功能差距。
2. **不可变列约束落地应用**：上次 `ImmutableColumnsConstraint` 引入后，本次 `FLINK-39339` 已开始利用该元数据优化 changelog mode 推断，特性持续深化。
3. **自适应调度器可观测性闭环**：上次 Web UI 引入了 Rescales 子页面（前端），本次后端 `RescaleContext` 记录机制补齐，形成完整闭环。
4. **OpenTelemetry 指标导出增强**：批量 gRPC 导出解决大规模指标上报的性能瓶颈，OpenTelemetry 集成趋于生产可用。
